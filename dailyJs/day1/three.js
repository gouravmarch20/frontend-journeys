// TODO: 
function encodedString (string, secret) {
  let encodedString = []

  for (let i = 0; i < string.length; i++) {
    // encodedString.push((string[i].charCodeAt() + 2).fromCharCode())
  }
  console.log(encodedString)
}
encodedString('neogcamp', 2)
