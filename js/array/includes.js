// includes : in simple array work  ==> if found true return else false
//In AOO => we require to use our owm loop

const items = [
    4, 9, 200, 67

];
const newItems = items.includes(200)
console.log(newItems)