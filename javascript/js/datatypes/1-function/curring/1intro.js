// take 1 args at a time , 
// create multiple utils ==> on base of it 

// !  Closore observation : here garbage collector not delete that var  ==> we can  used later that value ==> complex operator we store to avoid re-running same output logic